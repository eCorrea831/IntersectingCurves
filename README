You have two arrays of coordinates i.e. (x,y) pairs

1. The first array is a set of points that represents curve 1

2. The second array represents the curve 2

Write a program that can determine if the curves intersect.

Use the XCode project as a starting point for visualizing the problem.